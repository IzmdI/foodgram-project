version: '3.3'

volumes:
  postgres_data:
  static:
  media:

services:
  db:
    image: postgres:13.2
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./.env
  web:
    image: izmdi/foodgram:0.2
    volumes:
      - ./static:/var/nginx/static/
      - ./media:/var/nginx/media/
    restart: always
    depends_on:
      - db
    env_file:
      - ./.env
  nginx:
    image: nginx:1.19
    ports:
      - "80:80"
    volumes:
      - ./static:/var/nginx/static/
      - ./media:/var/nginx/media/
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - web